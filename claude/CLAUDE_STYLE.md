# CLAUDE_STYLE.md - Formatting Rules

Rules that apply to all output: code comments, documentation, markdown files, and responses.

## Typography

- Never use the em dash or long dash (--). Use a hyphen (-) instead.
- Never use the arrow symbol (->). Use -> instead.
