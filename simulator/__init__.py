"""Core simulator package for Norse Dice Dueler.

The package is intentionally split into three small surfaces:

- engine/data model modules such as `game_engine.py` and `game_state.py`
- agent implementations in [`simulator/agents`](/Users/rishisg2/Desktop/Rishi/coding/prototypes/norse_dice_dueler/simulator/agents)
- benchmark harnesses such as `l2_balance_matrix.py` and `l4_condition_pairs.py`

The harness modules are executable with `python -m simulator.<module_name>`.
"""
