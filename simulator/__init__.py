"""Benchmark harness package for Norse Dice Dueler.

`simulator/` is intentionally thin now. It only contains executable L0-L4
validation and balance runners. Core rules live in `game_mechanics/`, and
player policies live in `agents/`.
"""
