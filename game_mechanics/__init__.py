"""Core game rules, state objects, and data loaders.

This package holds the reusable mechanics layer that the simulator harnesses and
agents build on top of:

- `game_state.py` for immutable state snapshots and events
- `game_engine.py` for phase resolution and match execution
- `die_types.py` and `god_powers.py` for loading structured game data
"""
